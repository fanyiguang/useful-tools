package proxy

const (
	SSH         = "ssh"
	SSL         = "ssl"
	SOCKS5      = "socks5"
	HTTP        = "http"
	HTTPS       = "https"
	SHADOWSOCKS = "shadowsocks"
)

var (
	CheckIpUrls = "https://ipinfo.io/json"
)
